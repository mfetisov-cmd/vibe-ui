/**
 * Resistance factor for drag movement
 * Lower values make dragging feel more responsive but less smooth
 * Higher values make dragging feel smoother but less responsive
 */
export const RESISTANCE_FACTOR = 0.8;

/**
 * Default threshold for drag distance to trigger page change (in pixels)
 */
export const DEFAULT_DRAG_THRESHOLD = 50;

export const DEFAULT_GAP = 8;

export const DEFAULT_ITEMS_TO_SHOW = 1;
